import { SplitKeyPipePipe } from './split-key-pipe.pipe';

describe('SplitKeyPipePipe', () => {
  it('create an instance', () => {
    const pipe = new SplitKeyPipePipe();
    expect(pipe).toBeTruthy();
  });
});
